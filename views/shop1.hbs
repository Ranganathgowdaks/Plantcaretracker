<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Shop Page</title>
  <style>
      body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f8f9fa;
        }

        nav {
            background-color: #343a40;
            padding: 10px;
            text-align: center;
        }

        nav a {
            color: #fff;
            text-decoration: none;
            margin: 0 15px;
            font-size: 1.2em;
        }

        .offer-container {
            background-color: rgb(85, 226, 85);
            padding: 20px;
            text-align: center;
        }

        .offer-text {
            font-size: 1.5em;
            color: #343a40;
        }

        .main-container {
            display: grid;
            grid-template-columns: repeat(4, 1fr); /* Display 4 columns */
            grid-gap: 20px;
            margin-left: 6rem;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .plant-container {
            border: 2px solid #343a40;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
            cursor: pointer;
            width: 200px;
        }

        .plant-container:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .plant-image {
            height: 150px;
            width: 100%;
            background-position: center;
            background-size: cover;
            border-bottom: 2px solid #343a40;
        }

        .plant-details {
            padding: 15px;
        }

        .plant-name {
            font-size: 1.2em;
            color: #343a40;
            margin-bottom: 5px;
        }

        .plant-price {
            font-size: 1em;
            color: #6c757d;
            
           
        }

        .discount {
            color: #dc3545;
            font-size: 0.9em;
            margin-top: 5px;
            
        }
  </style>
</head>
<body>
  <header>
    <h1>Welcome to the Plant Shop!</h1>
  </header>

  <main class="main-container">
    {{#each plants}}
      <div class="plant-container">
        <div class="plant-image" style="background-image: url('{{img}}');"></div>
        <div class="plant-details">
          <p class="plant-name"><b>{{name}}</b></p>
          <p class="plant-price"> &#8377;{{price}} <span class="discount"> &#8377;{{discount}}</span></p>
          <button style="color: rgba(0, 128, 255, 0.947);">add to cart</button>
        </div>
      </div>
    {{/each}}
  </main>

  <footer>
    <p>&copy; 2023 Plant Shop. All rights reserved.</p>
  </footer>

  <script src="/scripts.js"></script>
</body>
</html>
